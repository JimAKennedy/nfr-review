apiVersion: apps/v1
kind: Deployment
metadata:
  name: bare-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bare-app
  template:
    metadata:
      labels:
        app: bare-app
    spec:
      containers:
        - name: app
          image: example/bare-app:latest
